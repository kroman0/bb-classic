language: python
python: 2.7
before_install:
  - pip install http://effbot.org/downloads/Imaging-1.1.6.tar.gz
install:
  - python bootstrap.py
  - bin/buildout -t 5 -Nq
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
script:
  - make
  - make test
